[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["**/*"]

[deploy]
# Removed startCommand to use Dockerfile's CMD which properly installs dependencies
healthcheckPath = "/health"
healthcheckTimeout = 120
